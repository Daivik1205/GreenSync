name: greensyncq
description: Queue-aware speed advisory app for GreenSync-Q V2I system.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mqtt_client: ^10.0.0
  supabase_flutter: ^2.0.0
  google_maps_flutter: ^2.5.0
  geolocator: ^11.0.0
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
